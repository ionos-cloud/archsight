name: Publish Helm Chart

on:
  push:
    branches:
      - main
    paths:
      - "chart/archsight/Chart.yaml"
      - ".github/workflows/helm.yml"

env:
  REGISTRY: ghcr.io

jobs:
  publish-helm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2 # Needed for git diff

      - name: Check if Chart.yaml version changed
        id: check-version
        run: |
          if git diff HEAD^ HEAD -- chart/archsight/Chart.yaml | grep -E '^\+version:'; then
            echo "version_changed=true" >> "$GITHUB_OUTPUT"
            VERSION=$(grep '^version:' chart/archsight/Chart.yaml | awk '{print $2}')
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          else
            echo "version_changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Helm
        if: steps.check-version.outputs.version_changed == 'true'
        uses: azure/setup-helm@v4

      - name: Log in to Container Registry
        if: steps.check-version.outputs.version_changed == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Helm chart dependencies
        if: steps.check-version.outputs.version_changed == 'true'
        run: |
          cd chart/archsight
          helm dependency update
          helm lint .

      - name: Package and push Helm chart
        if: steps.check-version.outputs.version_changed == 'true'
        run: |
          VERSION="${{ steps.check-version.outputs.version }}"

          echo "Packaging chart with version: $VERSION"
          helm package chart/archsight --version "$VERSION"

          CHART_PACKAGE="archsight-${VERSION}.tgz"
          CHART_REGISTRY="${{ env.REGISTRY }}/${{ github.repository }}/charts"
          
          # Ensure lowercase for OCI registry
          CHART_REGISTRY=$(echo "$CHART_REGISTRY" | tr '[:upper:]' '[:lower:]')

          echo "Pushing chart package: $CHART_PACKAGE to $CHART_REGISTRY"
          helm push "$CHART_PACKAGE" "oci://$CHART_REGISTRY"
